meta {
  name: Send Message Unauthorized
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: none
}

body:json {
  {
    "prompt": "Test message",
    "session_id": "test-session"
  }
}

assert {
  res.status: eq 401
}

tests {
  test("should return 401 without auth", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
