meta {
  name: Send Message Missing Session ID
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/chat
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "prompt": "Test message"
  }
}

assert {
  res.status: in [200, 400]
}

tests {
  test("should handle missing session ID", function() {
    const status = res.getStatus();
    expect([200, 400]).to.include(status);
  });
}
