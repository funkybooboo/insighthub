meta {
  name: Create Chat Session Unauthorized
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/workspaces/1/chat/sessions
  body: json
}

body:json {
  {
    "title": "Unauthorized Session"
  }
}

assert {
  res.status: eq 401
}

tests {
  test("should require authentication", function() {
    expect(res.getStatus()).to.equal(401);
  });
}