meta {
  name: Login Missing Password
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "testuser"
  }
}

assert {
  res.status: eq 400
}

tests {
  test("should return 400 for missing password", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
