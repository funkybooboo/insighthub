meta {
  name: Update RAG Config Invalid
  type: http
  seq: 16
}

patch {
  url: {{baseUrl}}/api/workspaces/{{workspaceId}}/rag-config
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "retriever_type": "invalid_type"
  }
}

assert {
  res.status: eq 400
}

tests {
  test("should return 400 for invalid retriever type", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
