meta {
  name: List Workspaces Unauthorized
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/workspaces
  body: none
  auth: none
}

assert {
  res.status: eq 401
}

tests {
  test("should return 401 without auth token", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
