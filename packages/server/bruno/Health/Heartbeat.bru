meta {
  name: Heartbeat
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/heartbeat
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

tests {
  test("should return 200 OK with no body", function() {
    expect(res.status).to.equal(200);
  });
}
