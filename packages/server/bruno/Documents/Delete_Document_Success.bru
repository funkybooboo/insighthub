meta {
  name: Delete Document Success
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/documents/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

assert {
  res.status: eq 200
}

tests {
  test("should delete document", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
