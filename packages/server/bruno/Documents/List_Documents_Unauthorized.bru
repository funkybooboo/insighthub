meta {
  name: List Documents Unauthorized
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/documents
  body: none
  auth: none
}

assert {
  res.status: eq 401
}

tests {
  test("should return 401 without auth", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
