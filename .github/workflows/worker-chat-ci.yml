name: Worker Chat CI

on:
    push:
        branches:
            - main
        paths:
            - 'packages/workers/chat/**'
            - 'packages/shared/**'
            - 'infra/**'
            - 'Taskfile.yml'
            - '.github/workflows/worker-chat-ci.yml'
    pull_request:
        branches:
            - main
        paths:
            - 'packages/workers/chat/**'
            - 'packages/shared/**'
            - 'infra/**'
            - 'Taskfile.yml'
            - '.github/workflows/worker-chat-ci.yml'

jobs:
    worker-chat-ci:
        runs-on: ubuntu-latest

        defaults:
            run:
                working-directory: packages/workers/chat

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Install Task
              run: sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/.local/bin

            - name: Add Task to PATH
              run: echo "$HOME/.local/bin" >> $GITHUB_PATH

            - name: Install Poetry
              uses: snok/install-poetry@v1.4.1
              with:
                  version: latest
                  virtualenvs-create: true
                  virtualenvs-in-project: true

            - name: Load cached venv
              id: cached-poetry-dependencies
              uses: actions/cache@v4
              with:
                  path: packages/workers/chat/.venv
                  key: venv-${{ runner.os }}-chat-${{ hashFiles('packages/workers/chat/poetry.lock') }}

            - name: Install dependencies
              if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
              run: |
                  # Install shared package first
                  cd packages/shared/python && poetry install --no-root
                  # Install worker dependencies
                  cd ../../../packages/workers/chat && task install

            - name: Format code (with auto-fix)
              run: task format

            - name: Run linter
              run: task lint

            - name: Type check with mypy
              run: task type-check

            - name: Run tests
              run: task test